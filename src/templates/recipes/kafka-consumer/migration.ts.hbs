import { MigrationInterface, QueryRunner } from 'typeorm';

export class KafkaConsumerReceipts1788600000000 implements MigrationInterface {
  async up(queryRunner: QueryRunner): Promise<void> {
    await queryRunner.query(`CREATE TABLE kafka_consumer_receipts (
      consumer_id varchar(200) NOT NULL,
      event_id varchar(200) NOT NULL,
      payload_hash char(64) NOT NULL,
      processed_at timestamptz NOT NULL DEFAULT now(),
      PRIMARY KEY (consumer_id, event_id)
    )`);
  }

  async down(queryRunner: QueryRunner): Promise<void> {
    await queryRunner.query('DROP TABLE kafka_consumer_receipts');
  }
}
